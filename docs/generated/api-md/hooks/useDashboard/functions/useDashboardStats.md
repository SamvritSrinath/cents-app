# Function: useDashboardStats()

> **useDashboardStats**(): `UseQueryResult`\<[`DashboardStats`](../interfaces/DashboardStats.md), `Error`\>

This month vs last month totals, percent change, and total expense count for the user.

## Returns

`UseQueryResult`\<[`DashboardStats`](../interfaces/DashboardStats.md), `Error`\>

## Throws

Error `"Not authenticated"` if there is no Supabase user.
