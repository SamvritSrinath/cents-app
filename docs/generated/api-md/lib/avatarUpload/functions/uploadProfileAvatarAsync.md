# Function: uploadProfileAvatarAsync()

> **uploadProfileAvatarAsync**(`userId`, `localUri`): `Promise`\<`string`\>

Upload or replace the user's avatar object at `{userId}/avatar.{ext}`.

## Parameters

### userId

`string`

### localUri

`string`

## Returns

`Promise`\<`string`\>
