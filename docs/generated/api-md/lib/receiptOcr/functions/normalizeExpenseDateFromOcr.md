# Function: normalizeExpenseDateFromOcr()

> **normalizeExpenseDateFromOcr**(`raw`): `string` \| `null`

Normalize OCR date strings to YYYY-MM-DD when possible.

## Parameters

### raw

`string` \| `null` \| `undefined`

## Returns

`string` \| `null`
